apiVersion: apps/v1
kind: Deployment
metadata:
  name: fiap-worker-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fiap-worker
  template:
    metadata:
      labels:
        app: fiap-worker
    spec:
      containers:
      - name: worker
        image: fiap-worker:v2
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8000
        envFrom:
        - configMapRef:
            name: fiap-config
        env:
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: fiap-secrets
              key: DB_PASSWORD
        - name: RABBITMQ_URL
          valueFrom:
            secretKeyRef:
              name: fiap-secrets
              key: RABBITMQ_URL
        volumeMounts:
        - name: shared-storage
          mountPath: /app/uploads
          subPath: uploads
        - name: shared-storage
          mountPath: /app/outputs
          subPath: outputs
        - name: shared-storage
          mountPath: /app/temp
          subPath: temp
      volumes:
      - name: shared-storage
        persistentVolumeClaim:
          claimName: shared-storage-pvc
